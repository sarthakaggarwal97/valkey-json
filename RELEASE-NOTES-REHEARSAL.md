# Fork-only release-notes rehearsal

This fixture simulates a user-visible JSON improvement for PR #80 validation:
JSON commands report path-limit violations with the configured limit and the
rejected path so clients can correct requests without inspecting server logs.

This file and behavior description exist only in the personal fork.
